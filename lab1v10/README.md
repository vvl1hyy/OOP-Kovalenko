# Лабораторна робота №1: Створення та робота з класами в C#

**Варіант №10:** Клас `Computer`  
**Виконав:** Коваленко Владислав (група ІПЗ 3/2)  

---

## Опис проєкту

Консольний застосунок на C# (.NET SDK), що демонструє роботу з класом `Computer`, реалізацію приватних полів, публічних властивостей з інкапсуляцією, базового конструктора та методу обчислення продуктивності.

## Результат роботи програми

<img width="803" height="70" alt="image" src="https://github.com/user-attachments/assets/e16a123c-9547-41d4-8005-c10603a5c1b0" />

---

## Відповіді на контрольні запитання

### 1. Що таке клас та об’єкт? Яка між ними різниця?
Клас — це опис (шаблон або креслення) того, які поля, властивості та методи матимуть об'єкти.  
Об’єкт — це конкретний екземпляр класу, який створюється в пам'яті за допомогою оператора `new` і містить реальні значення.  
Різниця: клас є лише схемою в коді, а об'єкт — сутністю, яка займає пам'ять під час виконання програми.

### 2. Для чого використовуються конструктори? Чи може клас існувати без них?
Конструктор використовується для автоматичної ініціалізації початкового стану об'єкта під час його створення.  
Клас може існувати без явно написаного конструктора. У такому випадку C# автоматично додає конструктор за замовчуванням без параметрів.

### 3. Чим відрізняється приватне поле від публічної властивості? Навіщо потрібна інкапсуляція?
Приватне поле (`private`) доступне лише всередині самого класу.  
Публічна властивість (`public`) надає контрольований доступ до даних за допомогою `get` та `set`.  
Інкапсуляція потрібна для приховування внутрішніх деталей реалізації та захисту даних об'єкта від некоректних змін ззовні.

## Структура класу `Computer`

### 1. Приватні поля
* `_cpu` (`string`) — назва процесора.
* `_ramGB` (`int`) — обсяг оперативної пам'яті в гігабайтах.
* `_storageGB` (`int`) — обсяг накопичувача в гігабайтах.

### 2. Властивості
* `CPU` — публічний доступ до назви процесора.
* `RAMGB` — публічний доступ до обсягу оперативної пам'яті.
* `StorageGB` — публічний доступ до обсягу накопичувача.

### 3. Конструктори
* **Параметризований:** `Computer(string cpu, int ramGB, int storageGB)` — ініціалізує об'єкт переданими значеннями.

### 4. Методи
* `RunBenchmark()` — виводить інформацію про запуск тесту та обчислює бали продуктивності.
* `GetInfo()` — повертає рядок із характеристиками комп'ютера.

---

## Результат виконання програми

<img width="803" height="70" alt="image" src="https://github.com/user-attachments/assets/e16a123c-9547-41d4-8005-c10603a5c1b0" />

---

## Запуск проєкту

1. Перейти в директорію проєкту:
   ```bash
   cd lab1v10

2. Запустити застосунок:
   ```bash
   dotnet run
